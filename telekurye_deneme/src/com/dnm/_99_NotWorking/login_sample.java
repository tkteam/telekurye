package com.dnm._99_NotWorking;

public class login_sample {
	// public static int getLoginStatus() throws IOException {
	//
	// String json = cevir(Info.USERNAME, Info.PASSWORD);
	// SyncResult data = gson.fromJson(json, SyncResult.class);
	// return data.getProcessStatus();
	//
	// }
	//
	// public static String cevir(String u, String p) throws IOException {
	//
	// URL url = new URL("http://rl.telekurye.com.tr/androidservices/Default.aspx?f=login&u=" + u + "&p=" + p + "&i=352728054859749");
	// URLConnection con = url.openConnection();
	// InputStream in = con.getInputStream();
	// String encoding = con.getContentEncoding();
	// encoding = encoding == null ? "UTF-8" : encoding;
	// String body = IOUtils.toString(in, encoding);
	// return body;
	//
	// }
}
